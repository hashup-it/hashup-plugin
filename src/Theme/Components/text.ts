import { theme } from "@chakra-ui/react";

export const text = {
    variants: {
        primary: {
            fontSize: '1rem',
            fontWeight: '700',
            color: theme.colors.white,
        }
    }
}